syntax = "proto3";
package CARTA;

import "enums.proto";
import "defs.proto";

message MomentResponse {
    repeated MomentImage output_files = 1;
    bool success = 2;
}

message MomentProgress {
    float progress = 1;
}