syntax = "proto3";
package CARTA;

import "enums.proto";
import "defs.proto";

message MomentResponse {
    bool success = 1;
    repeated MomentImage output_files = 2;
}

message MomentProgress {
    float progress = 1;
}