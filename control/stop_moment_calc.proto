syntax = "proto3";
package CARTA;
// Required for go generation
option go_package = "./cartaDefinitions";

message StopMomentCalc {
    // Stop the moment calculation with respect to the image file id
    sfixed32 file_id = 1;
}