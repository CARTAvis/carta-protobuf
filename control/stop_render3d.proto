syntax = "proto3";
package CARTA;

message StopRender3D {
    // Stop the 3D rendering for the render3d viewer id
    sfixed32 viewer_id = 1;
}

message CloseRender3D {
    // Close the 3D rendering for the render3d viewer id
    sfixed32 viewer_id = 1;
}