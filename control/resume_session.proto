syntax = "proto3";
package CARTA;

import "defs.proto";

message ResumeSession {
    repeated ImageProperties images = 1;
}

message ResumeSessionAck {
    bool success = 1;
    string message = 2;
}

