syntax = "proto3";
package CARTA;

import "defs.proto";

message ResumeSession {
    repeated ImageProperties images = 3;
}

message ResumeSessionAck {
    bool success = 1;
    string message = 2;
}

